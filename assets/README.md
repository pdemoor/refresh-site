# Video Asset

Add the autoplay video file here:

```text
assets/refresh-video.mp4
```

For mobile Safari autoplay, the file should be:

- MP4
- H.264 video
- AAC audio, or no audio track
- Reasonably compressed for mobile

The page uses a native muted, inline `<video>` element. This is much more reliable on iPhone Safari than a YouTube iframe.
